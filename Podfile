# Define the platform for the entire project (uncomment and specify the minimum iOS version)
platform :ios, '13.0'  # Ensure this line is uncommented and present

target 'MarketPlace-IOS' do
  use_frameworks!
  pod 'GooglePlaces'

  target 'MarketPlace-IOSTests' do
    inherit! :search_paths
  end

  target 'MarketPlace-IOSUITests' do
  end
end

target 'MarketPlace-IOS-Customers' do
  use_frameworks!
  pod 'GooglePlaces'
end
